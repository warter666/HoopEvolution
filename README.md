# HoopEvolution

让 AI 在自建的篮球世界里进化战术，并用 CSI 说清楚"为什么有效"。
这是篮球项目栈的研究型顶点：③ Basketball-CSI 与 ④ Evolution 的合体。

**定位：你是战术育种家。** 进化的方向盘在你手里——读考察报告、花行动点、
决定谁上谁下谁杂交，把随机变异雕成能通过试炼的冠军战术。
这是整个项目栈的"经营"支柱。

## 它的流水线

```
进化引擎(战术基因组) -> 回合模拟器(5v5) -> fitness(双防守平均ppp)
        ^                                        |
        └────── 选择/交叉/变异 <──── 记分 <──────┘
                                                 v
                          CSI 解剖报告(为什么有效) + tkinter 回放
```

四个值得一说的设计决定：

1. **命中模型是标定出来的，不是编的。** 启动时跨仓库调用 shotlab 物理引擎，
   对 6 个距离锚点各跑 120 次蒙特卡洛，得出"空位命中率-距离"曲线；
   shotlab 缺席时退化为文献表并显式声明来源。
2. **战术必须在两种防守下得分。** 人盯人有反应延迟（空位的物理来源），
   联防哨位随球滑动。fitness 取两者平均，防止进化出只会打一种防守的偏科战术。
3. **优势必须可解释。** 无球人贴近射手的防守人 0.6 米内，掩护成立、防守人被
   延缓 0.8 秒——进化找到的每个长处都能被事件流讲出来。
4. **解释可以被肉眼复核。** CSI 报告与 tkinter 回放读同一份事件流。

## 运行

```bash
python -m hoopevo manage                      # 赛季模式：你驾驶进化（推荐入口）
python -m hoopevo manage --auto --weeks 3     # 自动主管演示
python -m hoopevo evolve --gens 12 --pop 24   # 无人值守进化 + 冠军 CSI 报告
python -m hoopevo show                        # 快速进化 + 解剖报告
python -m hoopevo replay                      # 进化冠军 + 回放动画
python -m unittest discover -s tests          # 15 个测试
```

## 赛季模式怎么玩（可玩性核心）

十个训练周，你就是主教练兼育种家：

* 每周自动考察全队：每支战术跑 12 个回合（两种防守各半），
  报告给出 `ppp 均值 ± 噪声`——样本少的时候那个 ± 可能比差距还大；
* 每周 2 点行动点，花在哪是核心决策：**重点考察**（加样本降噪，看清一支
  战术的真实实力）/ **杂交**（两支优等生混血）/ **变异**（赌一把新东西）/
  **淘汰换血**（池子满了必须割舍）/ **深度解剖**（免费，每周一次的完整 CSI 报告）；
* 十周之后，**你亲自点名**谁代表球队出战三项试炼——均值最高未必是最优解，
  有时你要押注一支"还没看清但上限更高"的战术；
* 试炼：大心脏（压哨窗口命中率）、破联防、团队篮球（传球与掩护量）。
  三项全过评级 S。育种方向从此有了靶子：想拿 S，就得照着试炼雕战术。

## 一次真实的输出

进化 10 代后，冠军战术把射手摆在弧顶、PF 上前掩护，专打联防外线软肋
（对联防 3.00 ppp，对人盯人 1.12）。CSI 报告节选：

```
样本 24 回合 · 命中率 54% · ppp 1.62 · 人盯人 1.25 / 联防 2.00
为什么有效：
* 射手终结点: 弧顶 24（平均出手 9.1 m，平均防守距离 2.41 m）
* 掩护 1.5 次/回合（被延缓的防守人给射手制造出手空间）
  t= 0.9  C 掩护成立：PF 的防守人被延缓 0.8s
  t= 5.5  PF 弧顶出手 9.1 m · 最近防守 0.9 m · 命中概率 0.41 -> 命中
```

## 技术难点与取舍

| 问题 | 本作的做法 | 常见做法 | 为什么不用常见的 |
|---|---|---|---|
| 命中模型可信度 | shotlab 引擎对 6 锚点 × 120 次 MC 标定，缺失时文献表回退并声明 | 硬编码命中率表 | 让物理贯穿到底；标定来源在输出里可见，数字可追溯 |
| 精英保留失效 | 固定 tid：评估种子只由基因身份决定 | 每代重新随机评估 | 评估种子随代数变化会让 best 曲线抖动（踩过的坑）；固定后 best 单调不降且有测试保证 |
| 模拟器成本 | 位置型规则模拟（反应延迟、掩护延缓）+ 得分即停 | 5v5 物理引擎仿真 | 战术层面只需要位置动态；一局 <20ms，整个 GA 秒级跑完 |
| 报告可信度 | CSI 报告与 tkinter 回放读同一份事件流 | 报告与回放各自实现 | 解释和肉眼可见必须同源，否则报告不可复核 |
| 过拟合 | 双防守环境（人盯人 + 联防）平均 fitness | 单环境 fitness | 单环境会进化出"只会打一种防守"的偏科战术 |

## 已知局限

联防偏软（外线防守距离调制顶满），冠军会集中打它——真实但不平衡，
下一步是让防守也进化（攻防军备竞赛）。命中曲线 120 样本有约 ±5% 抽样噪声。
进化结果暂只输出到 stdout。

## 文档

策划书 `docs/DESIGN.md`；经验输入 `../BASKETBALL_LESSONS.md`；
总路线图 `../BASKETBALL_ROADMAP.md`。

## License

MIT
